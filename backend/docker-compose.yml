version: '3.8'

services:
  devassist-backend:
    build: .
    ports:
      - "${DEVASSIST_PORT}:${DEVASSIST_PORT}"
    environment:
      - NODE_ENV=development
      - HOST=127.0.0.1
      - PORT=${DEVASSIST_PORT}
    volumes:
      - .:/app
      - /app/node_modules
    command: npm start
