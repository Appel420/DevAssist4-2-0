version: '3.8'

services:
  devassist-backend:
    build: .
    ports:
      - "0000:0.0.0.0"
    environment:
      - NODE_ENV=development
      - PORT=0.0.0.0
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
